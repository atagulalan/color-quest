# Sound Files

Place your sound files here:

- `tick.mp3` - Clock tick sound that plays in the last 5 seconds of each level
- `music.mp3` - Background music that loops during gameplay

If the sound files are not found, the game will use fallback sounds generated using the Web Audio API.
